# ─────────────────────────────────────────────────────────────────────────────
# Law Chatbot — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# LLM (Groq via OpenAI SDK compatibility)
openai==1.54.0

# LangChain ecosystem — RAG pipeline
langchain==0.3.7
langchain-community==0.3.7
langchain-huggingface==0.1.2
langchain-qdrant==0.1.4

# Vector store
qdrant-client==1.12.0

# Embeddings (local, no API key needed)
sentence-transformers==3.3.1

# Database
pymongo==4.10.1

# Configuration & environment
python-dotenv==1.0.1
pydantic-settings==2.6.1
pydantic==2.9.2

# PDF loading
pypdf==5.1.0

# Web scraping (legal database ingestion)
beautifulsoup4==4.12.3
requests==2.32.3
lxml==5.3.0

# File uploads
python-multipart==0.0.12

# Background job queue (async ingestion of large PDFs)
rq==1.16.2
redis==5.2.0
